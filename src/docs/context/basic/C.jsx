import React from 'react';

const C = ({name}) => {
    return (
        <div>
            {name}
            <CharContext.Povider >
            </CharContext.Povider>
        </div>
    );
};

export default C;